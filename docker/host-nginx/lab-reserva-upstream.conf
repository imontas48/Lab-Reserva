# Upstream hacia el contenedor `web` de Lab-Reserva (compose publica en
# 127.0.0.1:8081). Va en /etc/nginx/conf.d/lab-reserva-upstream.conf porque
# `upstream` solo puede declararse a nivel http, no dentro de un server.
upstream lab_reserva_web {
    server 127.0.0.1:8081;
    keepalive 16;
}
